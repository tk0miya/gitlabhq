module WorkItems
  module Callbacks
    class Notifications < Base
      def before_update: () -> (nil | untyped)

      private

      def update_subscription: (untyped work_item, untyped subscription_params) -> untyped
    end
  end
end
