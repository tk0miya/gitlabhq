module API
  module Entities
    class Note < Grape::Entity
      # Only Issue and MergeRequest have iid
      NOTEABLE_TYPES_WITH_IID: ::Array["Issue" | "MergeRequest"]
    end

    # To be returned if the note was command-only
    class NoteCommands < Grape::Entity
    end
  end
end
